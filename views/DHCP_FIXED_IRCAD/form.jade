
extends ../layout
 

block CONTENT
  h1.page-header DHCP_FIXED_IRCAD
            a(href="/cms/edit_module/DHCP_FIXED_IRCAD")
                span(class="glyphicon glyphicon-cog pull-right")
  ul(class="nav nav-tabs nav-justified")
            li.active
                a(href="/module/DHCP_FIXED_IRCAD/form") New record
            li
                a(href="/module/DHCP_FIXED_IRCAD/view") List
  div.row
    div.col-md-6
      form(role="form",action="/module/DHCP_FIXED_IRCAD/post",method='POST')
        div.form-group
          label(for="IP") IP
          if(data['ip'] != undefined)
            input(name="ip",value="#{data['ip']}", type="text", class="form-control" ,id="ip", placeholder="")
          else
            input(name="ip", type="text", class="form-control" ,id="ip", placeholder="")
        div.form-group
          label(for="MAC") MAC
          if(data['mac'] != undefined)
            input(name="mac",value="#{data['mac']}", type="text", class="form-control" ,id="mac", placeholder="")
          else
            input(name="mac", type="text", class="form-control" ,id="mac", placeholder="")
        div.form-group
          label(for="hostname") hostname
          if(data['hostname'] != undefined)
            input(name="hostname",value="#{data['hostname']}", type="text", class="form-control" ,id="hostname", placeholder="")
          else
            input(name="hostname", type="text", class="form-control" ,id="hostname", placeholder="")
    
       
        button(type="submit" ,class="btn btn-success") Save