script:
  host  : 192.168.0.222
  user  : ${shaker['localhost']['user']}
  password  : ${shaker['localhost']['password']}

VAR:
    - VARIF

CONDITION2:
    if: True
    then: echo "2 Oui"
    else: echo "2 Non"

CONDITION:
    if : VARIF==100
    then: |
        echo "coucou"
        VARIF=100
        $$ sk_console.get VAR
        $$ rbox.cmd_if CONDITION2
    else: echo "caca"